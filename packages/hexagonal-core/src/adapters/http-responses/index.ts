// HTTP response primitives shared by concrete adapters. Currently the RFC 7807
// Problem Details schema reused for every error response.
export { ProblemDetailsSchema, ProblemJson } from "./problemDetails.schema.js";
