{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "generate": {
      "inputs": ["openapi/**"],
      "outputs": ["src/adapters/inbound/generated/**"]
    },
    "build": {
      "dependsOn": ["generate", "^build"],
      "inputs": [],
      "outputs": []
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "inputs": []
    },
    "lint:check": {
      "inputs": []
    },
    "format:check": {},
    "test": {
      "dependsOn": ["^build"],
      "inputs": [],
      "outputLogs": "errors-only"
    },
    "test:coverage": {
      "dependsOn": ["^build"],
      "inputs": [],
      "outputs": ["coverage"],
      "outputLogs": "errors-only"
    }
  }
}
