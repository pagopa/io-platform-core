name: Code Review

permissions:
  contents: read
  pull-requests: write

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize]
    paths:
      - apps/**
      - packages/**

jobs:
  js_code_review:
    uses: pagopa/dx/.github/workflows/js_code_review.yaml@723b8233cd9c48bac7cc9e796471d4e12d4f8e5c # main
    name: Code Review
    secrets: inherit
    permissions:
      contents: read
      pull-requests: write