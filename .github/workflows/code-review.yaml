name: Code Review

permissions:
  contents: read
  pull-requests: write

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize]
    paths:
      - apps/**
      - packages/**

jobs:
  js_code_review:
    uses: pagopa/dx/.github/workflows/js_code_review.yaml@625f02f382c33eb449cd94facb8e426bf7ae37ea # main
    name: Code Review
    secrets: inherit
    permissions:
      contents: read
      pull-requests: write